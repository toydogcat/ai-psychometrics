# 課程三：第 17 週 - 超參數調優與自動化管線 (Hyperparameter Tuning & Pipelines)

「手動調參」既低效又容易產生資料滲漏。本週我們將學習如何利用網格搜索與隨機搜索自動尋找模型的最優超參數。同時，我們將學習 Scikit-Learn 的核心武器——Pipeline，將預處理與建模封裝成一個不可分割的整體。

---

## 一、 單元講解 (Lecture) - 100 分鐘

### 1. 參數 (Parameters) vs. 超參數 (Hyperparameters)
- **參數**：模型內部學到的權重（如 $w, b$）。
- **超參數**：訓練前設定的控制項（如樹的深度、學習率、C 值）。

### 2. 網格搜索 (Grid Search)
- **原理**：設定候選值清單，窮舉所有可能的參數組合。
- **優缺點**：保證在設定範圍內找到最優，但面對參數多時會發生「維度災難」。

### 3. 隨機搜索 (Randomized Search)
- **原理**：在指定的參數分佈中隨機取樣 $N$ 組。
- **優勢**：運算成本可控，且在重要參數分佈不均時，往往比網格搜索更有效率。

### 4. Scikit-Learn Pipeline
- **痛點**：如果在 Split 前做標準化，測試集的資訊會透過均值滲漏給訓練集。
- **解決方案**：將 `Scaler` 與 `Model` 串聯。Pipeline 的 `fit` 會自動在訓練集上計算均值，`predict` 時自動套用。
- **代碼潔癖**：大幅簡化工作流，防止出錯。

### 5. 模型持久化 (Serialization)
- **工具**：`joblib` 與 `pickle`。
- **應用**：將訓練好的模型保存為檔案，以便在生產環境中載入預測。

---

## 二、 動手實作 (Lab) - 50 分鐘

### 任務：自動化建模大賽
1.  **構建 Pipeline**：
    - 建立一個包含 `SimpleImputer` (填充缺失值)、`StandardScaler` 與 `RandomForestClassifier` 的 Pipeline。
2.  **執行 GridSearchCV**：
    - 定義參數網格（如 `rf__n_estimators`, `rf__max_depth`）。
    - 在 Pipeline 上直接執行搜索。
3.  **比較 RandomizedSearchCV**：
    - 對比兩者的執行時間與獲得的最佳分數。
4.  **保存與載入模型**：
    - 將最優模型保存為 `.pkl` 檔。
    - 模擬重啟 Python，載入檔案並對新數據進行 `predict`。

---

## 三 island 知識點回顧 (KP)

- **[KP 3.17.1] 自動化搜索思想**：理解從手動試錯轉向自動化參數空間探索的必要性。
- **[KP 3.17.2] 搜尋空間設定**：掌握對數刻度（如 [0.001, 0.01, 0.1, 1, 10]）在調參中的應用。
- **[KP 3.17.3] Pipeline 的防滲漏機制**：深刻理解 Pipeline 如何確保轉換器 (Transformers) 的 Fit 過程嚴格限於訓練折疊。
- **[KP 3.17.4] 嵌套調參語法**：學會使用 `__` (雙下劃線) 存取 Pipeline 內部的 Estimator 參數。
- **[KP 3.17.5] 模型序列化陷阱**：明白 Python 版本與庫版本一致性對載入模型的重要性。

---

## 四、 課後測驗題庫 (Quiz) - 30 分鐘

### 1. 單選題 (Single Choice) - 10 題

1. **Q1**: 下列哪一個屬於「超參數」而非模型參數？
   - (A) 線性回歸的權重 $w$ (B) 神經網路的偏置 $b$ (C) 隨機森林中的樹木數量 (D) 支持向量機的支援向量
2. **Q2**: 網格搜索 (GridSearch) 的主要缺點是？
   - (A) 可能漏掉最優解 (B) 計算開銷隨參數數量呈指數增長 (C) 只能處理數值參數 (D) 不支持交叉驗證
3. **Q3**: 對於搜尋空間極大的情況，哪種搜索方式通常效率更高？
   - (A) Grid Search (B) Randomized Search (C) 窮舉法 (D) 手動調參
4. **Q4**: Scikit-Learn Pipeline 的 `fit` 方法依序呼叫了內部轉換器的？
   - (A) `fit` 與 `transform` (B) `predict` (C) `score` (D) `save`
5. **Q5**: Pipeline 中最後一個組件通常必須是一個？
   - (A) Transformer (轉換器) (B) Estimator (預測器/模型) (C) Imputer (D) Scaler
6. **Q6**: 在 Pipeline 中設置參數網格時，引用子組件 `rf` 的參數 `n_estimators` 應寫為？
   - (A) `rf.n_estimators` (B) `rf_n_estimators` (C) `rf__n_estimators` (D) `rf[n_estimators]`
7. **Q7**: 為了防止資料滲漏 (Data Leakage)，特徵縮放應在何時進行？
   - (A) 資料讀取時 (B) 整個資料集上 (C) 交叉驗證的每一折內部 (D) 模型訓練完成後
8. **Q8**: 下列哪一個函式庫最適合用於保存大型、包含大量 NumPy 數組的 sklearn 模型？
   - (A) `json` (B) `csv` (C) `joblib` (D) `txt`
9. **Q9**: 如果網格搜索的最佳參數位於你設定的邊界上（例如最大深度設為 10，結果也是 10），你應該？
   - (A) 停止搜索 (B) 擴大邊界繼續搜索 (C) 縮小邊界 (D) 更換模型
10. **Q10**: 隨機搜索相比網格搜索，其預算（迭代次數）是？
    - (A) 固定的 (B) 由用戶透過 `n_iter` 指定的 (C) 自動決定的 (D) 無窮大的

### 2. 多選題 (Multiple Choice) - 10 題

11. **Q11**: 超參數調優的目標包括？
    - (A) 尋找使模型泛化能力最強的配置 (B) 降低過擬合或欠擬合 (C) 自動優化模型權重 (D) 節省人工試錯時間
12. **Q12**: Scikit-Learn Pipeline 的核心價值有？
    - (A) 防止資料滲漏 (B) 代碼組織整潔 (C) 一次性對整個流水線進行調參 (D) 提高預測速度
13. **Q13**: 哪些參數適合放入隨機搜索的分佈中？
    - (A) 正則化強度 $C$ (B) 學習率 (C) 隱藏層神經元數量 (D) 是否包含截距 (布林值)
14. **Q14**: 關於 `GridSearchCV` 輸出的結果，正確的有？
    - (A) `best_params_` 儲存最佳組合 (B) `best_score_` 儲存最高分數 (C) `cv_results_` 儲存詳細搜索過程 (D) 它會自動在最後用全量訓練集重新擬合最優模型
15. **Q15**: 模型持久化 (Serialization) 的常見場景包括？
    - (A) 將模型從開發環境遷移到生產伺服器 (B) 保存耗時較長的訓練結果 (C) 進行版本控制 (D) 與前端 JavaScript 直接通訊 (需額外轉換)
16. **Q16**: 超參數搜索中常見的評價指標（Scoring）有？
    - (A) `accuracy` (B) `f1` (C) `neg_mean_squared_error` (D) `r2`
17. **Q17**: 關於 RandomizedSearchCV 的敘述，正確的有？
    - (A) 它不保證能找到網格點上的最優解 (B) 它能探索網格點之外的數值 (C) 它對非重要參數浪費的時間較少 (D) 它完全不需要交叉驗證
18. **Q18**: 設計 Pipeline 時，可以包含哪些步驟？
    - (A) 缺失值填充 (B) 特徵選擇 (C) 降維 (PCA) (D) 最終分類器
19. **Q19**: 哪些行為可能導致資料滲漏？
    - (A) 在劃分資料前計算全局均值並填補缺失值 (B) 在訓練集上選擇特徵，但在測試集上應用 (C) 根據測試集的分布來調整超參數 (D) 使用 Pipeline 封裝預處理
20. **Q20**: 在模型部署時，除了模型文件，通常還需要保存？
    - (A) 預處理器對象 (如 Scaler) (B) 特徵名稱清單 (C) 環境依賴版本 (D) 訓練時的所有日誌
### 3. 填充題 (Fill-in-the-blank) - 10 題

21. 訓練前需要手動設置的參數稱為 __________ 參數。
22. 窮舉所有參數組合的搜索方法是 __________ 搜索 (Grid Search)。
23. 隨機搜索中，可以通過參數 __________ 來控制嘗試多少組組合。
24. Pipeline 利用雙 __________ (__) 符號來指定子組件的參數。
25. __________ 工具比 Python 內建的 pickle 更適合處理大型數組模型。
26. 確保模型不會提前看到測試集資訊，是為了防止資料 __________。
27. Pipeline 的優點之一是將複雜的工作流封裝成一個 __________ (Estimator) 對象。
28. `GridSearchCV` 預設會執行 __________ 折交叉驗證。
29. 尋找最優超參數的過程在機器學習中常被稱為 __________。
30. 最佳超參數組合存儲在搜索對象的 __________ 屬性中。

---

## 五、 Q 矩陣 (Q-matrix)

| 題號 | KP 3.17.1 | KP 3.17.2 | KP 3.17.3 | KP 3.17.4 | KP 3.17.5 |
|---|:---:|:---:|:---:|:---:|:---:|
| Q1 | 1 | | | | |
| Q2 | | 1 | | | |
| Q3 | | 1 | | | |
| Q4 | | | 1 | | |
| Q5 | | | 1 | | |
| Q6 | | | | 1 | |
| Q7 | | | 1 | | |
| Q8 | | | | | 1 |
| Q9 | | 1 | | | |
| Q10 | | 1 | | | |
| Q11 | 1 | | | | |
| Q12 | | | 1 | | |
| Q13 | | 1 | | | |
| Q14 | 1 | 1 | | | |
| Q15 | | | | | 1 |
| Q16 | 1 | | | | |
| Q17 | | 1 | | | |
| Q18 | | | 1 | | |
| Q19 | | | 1 | | |
| Q20 | | | | | 1 |
| Q21 | 1 | | | | |
| Q22 | | 1 | | | |
| Q23 | | 1 | | | |
| Q24 | | | | 1 | |
| Q25 | | | | | 1 |
| Q26 | | | 1 | | |
| Q27 | | | 1 | | |
| Q28 | 1 | | | | |
| Q29 | 1 | | | | |
| Q30 | 1 | | | | |
