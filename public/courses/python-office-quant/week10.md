# 第 10 週：科學計算基石：NumPy 數值運算

## 一、 單元講解 (Lecture)
本單元將學習 Python 數據分析的底層基石 NumPy，掌握高效的數值計算與矩陣運算。 (100 分鐘)

### 1. NumPy 基礎與 ndarray (20 min)
- **為什麼使用 NumPy**: 向量化運算 vs. Python 原始迴圈的性能差異。
- **ndarray 對象**: 維度 (ndim)、形狀 (shape) 與數據類型 (dtype)。
- **創建數組**: `np.array()`, `np.zeros()`, `np.ones()`, `np.arange()`, `np.linspace()`。

### 2. 數組索引與切片 (20 min)
- **基礎切片**: 一維與多維數組的子集選取。
- **花式索引 (Fancy Indexing)**: 使用整數數組進行索引。
- **布林索引**: 利用邏輯條件篩選數據。

### 3. 維度變換與合併 (20 min)
- **形狀調整**: `reshape()`, `flatten()`, `ravel()`。
- **轉置與軸對換**: `transpose()` 與 `.T` 屬性。
- **合併與拆分**: `vstack()`, `hstack()`, `concatenate()`。

### 4. 廣播機制 (Broadcasting) (20 min)
- **廣播原則**: 了解不同形狀數組進行運算時的自動擴張規則。
- **維度匹配**: 理解後緣維度 (trailing dimension) 的重要性。
- **實戰案例**: 數組與標量、數組與向量的運算。

### 5. 數學運算與統計函數 (20 min)
- **通用函數 (ufunc)**: `sin()`, `exp()`, `sqrt()` 等元素級運算。
- **統計聚合**: `sum()`, `mean()`, `std()`, `max()`, `min()` 及其 `axis` 參數的應用。
- **線性代數基礎**: 矩陣乘法 `dot()` 或 `@` 運算符。

---

## 二、 動手實作 (Lab)
練習使用 NumPy 進行高效的數據運算。 (50 分鐘)

### 1. 百萬級數據運算對比 (20 min)
- **任務**: 分別使用 Python 列表與 NumPy 數組計算 100 萬個隨機數的平方和，並對比執行時間。
- **要求**: 使用 `time` 模組記錄差異。

### 2. 矩陣運算實踐 (15 min)
- **任務**: 創建兩個 3x3 矩陣，計算它們的點積（矩陣乘法），並求出結果矩陣的特徵值。

### 3. 圖像灰度處理 (模擬) (15 min)
- **任務**: 將一個隨機生成的 (100, 100, 3) 三維數組（代表 RGB 圖片）轉換為 (100, 100) 的二維灰度數組。
- **要求**: 使用加權平均公式：`0.299R + 0.587G + 0.114B`。

---

## 三、 本週知識點回顧 (KP)
- **[KP 10.1]**: 理解 ndarray 的記憶體佈局與數據類型 (dtype) 的重要性。
- **[KP 10.2]**: 掌握形狀變換（reshape, transpose）與數組合併的技術。
- **[KP 10.3]**: 深入理解廣播規則，能預判不同形狀數組運算的結果。
- **[KP 10.4]**: 熟練運用線性代數模組 (linalg) 與矩陣乘法運算。
- **[KP 10.5]**: 掌握數據聚合函數，並能靈活運用軸 (Axis) 參數處理多維數據。

---

## 四、 課後測驗題庫 (Quiz)

### 1. 單選題 (Single Choice, SC)
1. 對於一個形狀為 (3, 4) 的二維數組，執行 `arr.sum(axis=0)` 後，結果的形狀是？ (A) (3,) (B) (4,) (C) (12,) (D) (3, 4)
2. 下列哪個方法可以用來將多維數組完全展開為一維數組？ (A) `reshape(-1)` (B) `flatten()` (C) `ravel()` (D) 以上皆是
3. NumPy 中創建一個 0 到 10（不含 10）、步長為 2 的數組，語法是？ (A) `np.arange(0, 10, 2)` (B) `np.linspace(0, 10, 2)` (C) `np.array(0, 10, 2)` (D) `np.range(10, step=2)`
4. 兩個形狀分別為 (3, 1) 和 (1, 4) 的數組進行加法運算，結果的形狀是？ (A) 無法運算 (B) (3, 4) (C) (3, 1) (D) (1, 4)
5. 矩陣乘法（點積）在 Python 3.5+ 中可以使用哪個運算符？ (A) `*` (B) `**` (C) `@` (D) `&`
6. `arr.dtype` 屬性的作用是？ (A) 查看數組維度 (B) 查看數組元素類型 (C) 查看數組總個數 (D) 修改數組名稱
7. 下列哪種方式創建的是單位矩陣？ (A) `np.ones((3, 3))` (B) `np.eye(3)` (C) `np.zeros((3, 3))` (D) `np.empty((3, 3))`
8. `np.linspace(0, 1, 5)` 產生的數組包含幾個元素？ (A) 4 (B) 5 (C) 6 (D) 無數個
9. 如何獲取數組中最大值的「索引」位置？ (A) `max()` (B) `argmax()` (C) `where_max()` (D) `top()`
10. `arr.T` 的作用等同於？ (A) `arr.transpose()` (B) `arr.reshape()` (C) `arr.inverse()` (D) `arr.copy()`

### 2. 多選題 (Multiple Choice, MC)
11. NumPy 廣播機制在哪些情況下會生效？ (A) 兩個數組維度完全相同 (B) 其中一個數組的維度為 1 (C) 兩個數組維度數量不等，但後緣維度長度相符 (D) 任何形狀的兩個數組都可以廣播
12. 關於 `ndarray` 的屬性，正確的有？ (A) `ndim`: 維度數量 (B) `shape`: 各維度的長度 (C) `size`: 元素總數 (D) `itemsize`: 每個元素佔用的字節數
13. 下列哪些函數可以用來組合數組？ (A) `np.vstack()` (B) `np.hstack()` (C) `np.concatenate()` (D) `np.append()`
14. 關於布林索引，正確的有？ (A) 可以使用 `&`, `|`, `~` 運算符 (B) 索引數組必須是布林類型 (C) 返回的是符合條件的原數組視圖 (D) 返回的是符合條件的數據拷貝
15. NumPy 常用的統計聚合函數包括？ (A) `mean()` (B) `median()` (C) `std()` (D) `var()`
16. 下列哪些方法會改變數組本身的形狀？ (A) `reshape()` (返回新數組) (B) `resize()` (C) `transpose()` (返回視圖) (D) 修改 `.shape` 屬性
17. 關於 NumPy 的數據類型 (dtype)，正確的有？ (A) 支援 `int64` (B) 支援 `float32` (C) 支援 `complex` (複數) (D) 一個數組內可以混用不同類型
18. 下列哪些操作屬於「通用函數 (ufunc)」？ (A) `np.sin()` (B) `np.exp()` (C) `np.add()` (D) `np.log()`
19. 關於 `np.nan` (Not a Number)，正確的有？ (A) 它的類型是 float (B) `nan == nan` 結果為 False (C) 常用於表示缺失數據 (D) `np.isnan()` 可以檢測它
20. 使用 NumPy 相比 Python List 的優勢有？ (A) 更省內存 (B) 運算速度更快 (C) 代碼更簡潔 (D) 支援更豐富的數學函數

### 3. 填充題 (Fill-in-the-blank, Fill)
21. 在 NumPy 中，計算兩個數組 `A` 與 `B` 的矩陣內積應使用 `np.________(A, B)`。
22. 獲取數組各維度長度的屬性是 __________。
23. `np.zeros((2, 3))` 創建的是一個 __________ 行 __________ 列的零矩陣。
24. 計算數組標準差的函數是 `np.________()`。
25. 要將數組中的 `nan` 替換為 0，可以使用 `np.________(arr, 0)`。
26. 數組轉置的快捷屬性是 __________。
27. `np.arange(1, 5)` 產生的數組內容是 __________。
28. 若要獲取符合條件 `arr > 5` 的所有元素，代碼為 `arr[________]`。
29. 合併數組時，`axis=0` 代表 __________ 方向的合併。
30. NumPy 數組要求所有元素的 __________ 必須相同。

---

## 五、 Q 矩陣 (Q-matrix)
| 題目 | KP10.1 | KP10.2 | KP10.3 | KP10.4 | KP10.5 |
|---|---|---|---|---|---|
| SC1 | 0 | 0 | 0 | 0 | 1 |
| SC2 | 0 | 1 | 0 | 0 | 0 |
| SC3 | 1 | 0 | 0 | 0 | 0 |
| SC4 | 0 | 0 | 1 | 0 | 0 |
| SC5 | 0 | 0 | 0 | 1 | 0 |
| SC6 | 1 | 0 | 0 | 0 | 0 |
| SC7 | 1 | 0 | 0 | 0 | 0 |
| SC8 | 1 | 0 | 0 | 0 | 0 |
| SC9 | 0 | 0 | 0 | 0 | 1 |
| SC10| 0 | 1 | 0 | 0 | 0 |
| MC11| 0 | 0 | 1 | 0 | 0 |
| MC12| 1 | 0 | 0 | 0 | 0 |
| MC13| 0 | 1 | 0 | 0 | 0 |
| MC14| 1 | 0 | 0 | 0 | 0 |
| MC15| 0 | 0 | 0 | 0 | 1 |
| MC16| 0 | 1 | 0 | 0 | 0 |
| MC17| 1 | 0 | 0 | 0 | 0 |
| MC18| 1 | 0 | 0 | 0 | 0 |
| MC19| 1 | 0 | 0 | 0 | 0 |
| MC20| 1 | 0 | 0 | 0 | 0 |
| Fill21| 0 | 0 | 0 | 1 | 0 |
| Fill22| 1 | 0 | 0 | 0 | 0 |
| Fill23| 1 | 0 | 0 | 0 | 0 |
| Fill24| 0 | 0 | 0 | 0 | 1 |
| Fill25| 0 | 0 | 0 | 0 | 1 |
| Fill26| 0 | 1 | 0 | 0 | 0 |
| Fill27| 1 | 0 | 0 | 0 | 0 |
| Fill28| 1 | 0 | 0 | 0 | 0 |
| Fill29| 0 | 1 | 0 | 0 | 0 |
| Fill30| 1 | 0 | 0 | 0 | 0 |
