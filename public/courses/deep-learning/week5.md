# 課程四：第 5 週 - 計算機視覺 (CV) 革命：卷積神經網絡 (CNN)

為什麼人類可以一眼認出貓，而電腦卻覺得是一堆亂序的像素？本週我們將學習卷積神經網絡 (CNN)，理解它如何透過「局部連接」與「權重共享」模擬人類視覺系統，成為影像辨識領域的絕對霸主。

---

## 一、 單元講解 (Lecture) - 100 分鐘

### 1. MLP 處理圖像的困境
- **維度爆炸**：一張 1080p 圖片會產生數百萬個輸入，全連接層參數過多。
- **空間資訊丟失**：將圖像展平會破壞鄰近像素間的結構關係。

### 2. 卷積操作 (Convolution) 的直覺
- **過濾器 (Filter/Kernel)**：在圖像上滑動，提取邊案、紋理等特徵。
- **特徵圖 (Feature Map)**：卷積運算的輸出。

### 3. CNN 的三大核心支柱
- **局部連接 (Local Connectivity)**：神經元只關注一小塊區域。
- **權重共享 (Weight Sharing)**：同一個過濾器在整張圖上掃描，大幅減少參數量。
- **空間下採樣 (Pooling)**：降低解析度，增強特徵的不變性。

### 4. 卷積層細節
- **步長 (Stride)**：滑動的跨度。
- **填充 (Padding)**：解決邊緣像素被忽視及尺寸縮減的問題。
- **多通道卷積**：如何處理 RGB 三原色。

### 5. 池化層 (Pooling Layer)
- **最大池化 (Max Pooling)**：取區域內最強信號。
- **平均池化 (Average Pooling)**：取區域平均值。

---

## 二、 動手實作 (Lab) - 50 分鐘

### 任務：MNIST 手寫數字 CNN 實作
1.  **網絡定義**：
    - 使用 PyTorch 定義包含兩層卷積、兩層池化的網絡。
2.  **維度計算驗證**：
    - 手動計算每一層輸出張量的 Shape，並與代碼運行結果核對。
3.  **訓練與觀察**：
    - 比較同樣參數量的 MLP 與 CNN 在 MNIST 上的準確率與收斂速度。
4.  **卷積核可視化**：
    - 提取第一層卷積核的權重並顯示為圖像，觀察它們是在尋找橫線還是直線。

---

## 三 island 知識點回顧 (KP)

- **[KP 4.5.1] 感受野 (Receptive Field)**：理解神經元在原始圖像上能看到的區域大小。
- **[KP 4.5.2] 參數量對比計算**：掌握 CNN 如何透過權重共享將參數量降低數個數量級。
- **[KP 4.5.3] 卷積輸出尺寸公式**：熟練掌握 $O = \lfloor \frac{I - K + 2P}{S} \rfloor + 1$。
- **[KP 4.5.4] 池化層的降維與平移不變性**：明白池化如何讓模型對物體微小位移不敏感。
- **[KP 4.5.5] 層次化特徵提取**：理解淺層學線條、深層學語義（眼睛、鼻子、臉）的過程。

---