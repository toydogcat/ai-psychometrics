# 課程四：第 4 週 - PyTorch 實戰：張量運算與自動微分 (PyTorch Mastery)

紙上談兵結束，實戰開始！本週我們將進入業界最主流的深度學習框架之一：PyTorch。我們將理解什麼是張量，並掌握自動微分引擎 Autograd，這是深度學習自動化訓練的心臟。

---

## 一、 單元講解 (Lecture) - 100 分鐘

### 1. 深度學習框架之王：為什麼選擇 PyTorch？
- **動態圖 (Dynamic Graph)**：程式碼即圖，調試方便。
- **Pythonic 體驗**：與 Python 語法高度一致。
- **強大的生態**：HuggingFace, Torchvision 的支撐。

### 2. 張量 (Tensor) 基礎
- **定義**：多維數組。
- **屬性**：Shape, Dtype, Device。
- **運算**：矩陣乘法、廣播機制、索引與切片。

### 3. Autograd：自動微分引擎
- **`requires_grad`**：告訴 PyTorch 追蹤這顆變數。
- **計算圖的構建**：底層是如何紀錄運算過程的。
- **`.backward()`**：一鍵求導。

### 4. PyTorch 建模核心三部曲
- **`nn.Module`**：定義網路結構。
- **`optim`**：選擇優化器（SGD, Adam）。
- **訓練循環**：Forward -> Loss -> Zero_grad -> Backward -> Step。

### 5. GPU 加速與 CPU 交互
- **CUDA**：如何將運算從處理器移至顯卡。

---

## 二、 動手實作 (Lab) - 50 分鐘

### 任務：手寫線性回歸器 (PyTorch 版)
1.  **張量操縱**：
    - 建立 1D 到 4D 的張量。
    - 練習 `view()`, `reshape()`, `transpose()` 等重塑操作。
2.  **自動微分測試**：
    - 定義一個複雜函數 $y = x^2 + \sin(x)$。
    - 計算 $x=1$ 時的導數並與解析解對比。
3.  **線性回歸實作**：
    - 不使用高級封裝，僅使用 Tensor 和 Autograd 實現一個線性回歸訓練。
4.  **模型封裝**：
    - 學習繼承 `nn.Module` 構建模型。
    - 跑通一個簡單的 MNIST 分類流程。

---

## 三 island 知識點回顧 (KP)

- **[KP 4.4.1] Tensor 與 Numpy 的互操作性**：掌握底層內存共享的轉換機制。
- **[KP 4.4.2] 動態計算圖生命週期**：理解圖是在前向傳播時構建，後向傳播後銷毀（預設）。
- **[KP 4.4.3] 梯度清零的必要性**：明白 PyTorch 預設會累加梯度的設計初衷。
- **[KP 4.4.4] nn.Module 的層級設計**：理解 Parameter 與 Buffer 的管理方式。
- **[KP 4.4.5] 硬件部署意識**：掌握 `.to(device)` 進行設備切換的標準化代碼。

---

## 四、 課後測驗題庫 (Quiz) - 30 分鐘

### 1. 單選題 (Single Choice) - 10 題

1. **Q1**: PyTorch 的計算圖類型主要是？
   - (A) 靜態圖 (B) 動態圖 (C) 二叉圖 (D) 循環圖
2. **Q2**: 將 PyTorch 張量 `t` 轉回 NumPy 數組的方法是？
   - (A) `t.to_numpy()` (B) `t.numpy()` (C) `t.value()` (D) `np.array(t)`
3. **Q3**: 若要計算某個張量的梯度，必須在建立時設置？
   - (A) `grad=True` (B) `requires_grad=True` (C) `track=True` (D) `diff=True`
4. **Q4**: 哪一個方法可以用於重塑張量維度而不改變其元素總數？
   - (A) `squeeze()` (B) `reshape()` (C) `cat()` (D) `chunk()`
5. **Q5**: 在訓練循環中，`optimizer.zero_grad()` 的作用是？
   - (A) 將模型權重歸零 (B) 清空上一次迭代殘留的梯度 (C) 將損失歸零 (D) 停止梯度更新
6. **Q6**: PyTorch 中負責所有神經網路模組的基類是？
   - (A) `nn.Base` (B) `nn.Module` (C) `nn.Network` (D) `nn.Object`
7. **Q7**: 執行 `x.to("cuda")` 的前提是？
   - (A) 內存足夠 (B) 安裝了 NVIDIA GPU 及對應驅動 (C) 安裝了 CPU (D) 代碼沒有 Bug
8. **Q8**: 張量的廣播機制（Broadcasting）允許不同形狀的張量在什麼條件下運算？
   - (A) 維度完全相同 (B) 末尾維度長度相符或其中之一為 1 (C) 元素總數相同 (D) 隨機
9. **Q9**: 下列哪一個模組包含了常用的優化算法如 Adam, SGD？
   - (A) `torch.nn` (B) `torch.optim` (C) `torch.autograd` (D) `torch.utils`
10. **Q10**: 執行 `loss.backward()` 後，梯度儲存在變數的哪個屬性中？
    - (A) `.gradient` (B) `.grad` (C) `.d` (D) `.delta`

### 2. 多選題 (Multiple Choice) - 10 題

11. **Q11**: PyTorch 張量的常見維度 (Shape) 操作包括？
    - (A) `view` (B) `permute` (C) `unsqueeze` (D) `append`
12. **Q12**: 關於 `nn.Module` 的描述，正確的有？
    - (A) 必須實現 `forward` 方法 (B) 初始化時需調用 `super().__init__()` (C) 可以嵌套其他 Module (D) 自動管理內部的 Parameters
13. **Q13**: 哪些情況下不需要計算梯度？
    - (A) 訓練模型時 (B) 測試模型（推理）時 (C) 特徵提取時 (D) 設置了 `with torch.no_grad():`
14. **Q14**: PyTorch 的優點有？
    - (A) 社群資源豐富 (B) 調試與 Python 一樣簡單 (C) 支持分佈式訓練 (D) 靜態圖優化比動態圖更容易
15. **Q15**: 深度學習中的常用數據類型 (Dtype) 包括？
    - (A) `torch.float32` (B) `torch.int64` (C) `torch.bool` (D) `torch.string` (PyTorch 不直接支持字串張量)
16. **Q16**: `DataLoader` 的功能包括？
    - (A) 批量加載 (B) 數據打亂 (C) 多線程讀取 (D) 損失計算
17. **Q17**: 廣播機制（Broadcasting）的規則？
    - (A) 從最後一個維度往前對齊 (B) 長度相同則匹配 (C) 長度不同但其中一個為 1 則擴展 (D) 長度不同且都不為 1 則報錯
18. **Q18**: 常見的張量合併操作有？
    - (A) `torch.cat` (B) `torch.stack` (C) `torch.merge` (D) `torch.join`
19. **Q19**: 在 GPU 上運算的正確流程？
    - (A) 將模型移至 GPU (B) 將輸入數據移至 GPU (C) 計算結果後若需打印需移回 CPU (D) 必須使用雙显卡
20. **Q20**: `optim.Adam` 相比 `optim.SGD` 的特點？
    - (A) 內建動量機制 (B) 自適應學習率 (C) 收斂通常更快 (D) 絕對不會陷入局部最優

### 3. 填充題 (Fill-in-the-blank) - 10 題

21. PyTorch 中的基本數據結構是 __________。
22. 檢測系統是否支持 GPU 運算的代碼是 `torch.cuda.is____________()`。
23. 獲取張量維度大小的方法是 `._________()`。
24. 在 PyTorch 中，一鍵計算梯度的函式是 `.___________()`。
25. __________ 是 PyTorch 用於自動求導的底層引擎。
26. 如果不希望某個操作被紀錄在計算圖中，應使用 `torch.no__________`。
27. 將張量展平為一維的快捷方法是 `.__________()`。
28. `nn.__________Loss` 是多分類任務中最常用的損失函數類別。
29. 更新參數的代碼是 `optimizer.__________()`。
30. PyTorch 的核心庫名稱是 __________。

---

## 五、 Q 矩陣 (Q-matrix)

| 題號 | KP 4.4.1 | KP 4.4.2 | KP 4.4.3 | KP 4.4.4 | KP 4.4.5 |
|---|:---:|:---:|:---:|:---:|:---:|
| Q1 | | 1 | | | |
| Q2 | 1 | | | | |
| Q3 | | 1 | | | |
| Q4 | 1 | | | | |
| Q5 | | | 1 | | |
| Q6 | | | | 1 | |
| Q7 | | | | | 1 |
| Q8 | 1 | | | | |
| Q9 | | | 1 | | |
| Q10 | | 1 | | | |
| Q11 | 1 | | | | |
| Q12 | | | | 1 | |
| Q13 | | 1 | | | |
| Q14 | | 1 | | | |
| Q15 | 1 | | | | |
| Q16 | | | | | 1 |
| Q17 | 1 | | | | |
| Q18 | 1 | | | | |
| Q19 | | | | | 1 |
| Q20 | | | 1 | | |
| Q21 | 1 | | | | |
| Q22 | | | | | 1 |
| Q23 | 1 | | | | |
| Q24 | | 1 | | | |
| Q25 | | 1 | | | |
| Q26 | | 1 | | | |
| Q27 | 1 | | | | |
| Q28 | | | | 1 | |
| Q29 | | | 1 | | |
| Q30 | 1 | | | | |
