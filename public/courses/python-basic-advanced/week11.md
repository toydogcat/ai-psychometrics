# 課程一：第 11 週 - 物件導向程式設計 II：繼承、多型與方法

本週我們將探討物件導向的高階特性：如何透過繼承重用程式碼、如何處理複雜的多重繼承衝突，以及如何利用「多型」寫出最具彈性的程式碼。

---

## 一、 單元講解 (Lecture) - 100 分鐘

### 1. 繼承 (Inheritance) 的基礎與程式碼重用
- **父類別 (Base/Parent Class)** 與 **子類別 (Derived/Child Class)**。
- **Is-a 關係**：子類別「是一個」父類別的特殊化版本。
- **覆蓋 (Overriding)**：子類別重新定義父類別的方法以改變行為。

### 2. 多重繼承與 MRO (Method Resolution Order)
- **多重繼承的挑戰**：當多個父類別有同名方法時，Python 如何決定呼叫順序？
- **C3 線性化算法**：解析 Python 3 的 MRO 機制。
- **`cls.__mro__`**：查看方法的搜尋路徑。

### 3. `super()` 的深度解析
- **為什麼不直接用 `Parent.method(self)`？**：處理多重繼承時的協同呼叫。
- **`super()` 的工作原理**：它不是呼叫父類別，而是呼叫 MRO 中的下一個類別。
- **正確初始化**：在繼承鏈中確保所有層級都被正確初始化。

### 4. 多型 (Polymorphism) 與 鴨子型別 (Duck Typing)
- **多型概念**：同一個介面，不同實作。
- **鴨子型別 (Duck Typing)**："If it walks like a duck and quacks like a duck, it’s a duck."
- **實務應用**：只要物件有 `read()` 方法，它就能被當作檔案處理。

### 5. 方法型態：實體、類別與靜態方法
- **實體方法**：第一個參數是 `self`，存取實體狀態。
- **類別方法 (@classmethod)**：第一個參數是 `cls`，常作為「替代構造函式」(Factory Method)。
- **靜態方法 (@staticmethod)**：不接收 `self` 或 `cls`，純粹將邏輯封裝在類別命名空間內。

---

## 二、 動手實作 (Lab) - 50 分鐘

### 任務：開發「多功能動物園與支付系統」
1.  **建立繼承體系**：
    - 定義父類別 `Animal`，包含 `make_sound()` 方法（丟出 `NotImplementedError`）。
    - 建立子類別 `Dog` 與 `Cat` 覆蓋該方法。
    - 建立 `Flying` 混入類別 (Mixin)，提供 `fly()` 功能。
    - 建立 `Bat` 類別同時繼承 `Animal` 與 `Flying`。
2.  **觀察 MRO**：印出 `Bat.__mro__` 並分析搜尋順序。
3.  **實作多型函式**：
    - 撰寫 `zoo_concert(animals)`，接收物件列表，無論是什麼動物都呼叫 `make_sound()`。
4.  **類別方法應用**：
    - 在 `Animal` 中實作一個類別方法 `from_string(data)`，能將 "Dog-Buddy" 這種字串解析並回傳 `Dog` 實例。

---

## 三、 本週知識點回顧 (KP)

- **[KP 11.1] 繼承層級**：透過 `class Child(Parent)` 建立連結，子類別繼承父類別的所有非私有屬性。
- **[KP 11.2] MRO (搜尋路徑)**：Python 使用深度優先、從左至右的線性化順序解決方法衝突。
- **[KP 11.3] super() 協同機制**：在多重繼承中，`super()` 確保每個父類別的方法僅被執行一次。
- **[KP 11.4] 鴨子型別**：Python 不檢查型別，只檢查行為 (Behavior Over Type)。
- **[KP 11.5] 靜態與類別方法**：區分狀態相依性：實體狀態 (self)、類別狀態 (cls)、無狀態 (staticmethod)。

---

## 四、 課後測驗題庫 (Quiz) - 30 分鐘

### 1. 單選題 (Single Choice) - 10 題

1. **Q1**: 在 Python 3 中，查看一個類別 MRO 順序的方法是？
   - (A) `obj.mro()` (B) `ClassName.__mro__` (C) `ClassName.path()` (D) `help(ClassName)`
2. **Q2**: 關於 `super()`，下列敘述何者正確？
   - (A) 它總是指向父類別 (B) 它指向 MRO 列表中的下一個類別 (C) 它只能在 `__init__` 中使用 (D) 呼叫 `super()` 不需要括號
3. **Q3**: 哪一個裝飾器用於定義不需要存取實體或類別屬性的方法？
   - (A) `@classmethod` (B) `@staticmethod` (C) `@property` (D) `@abstractmethod`
4. **Q4**: 若 `class C(A, B): pass`，且 A, B 都有 `f()`，呼叫 `C().f()` 時優先呼叫哪一個？
   - (A) A 的 `f()` (B) B 的 `f()` (C) 報錯 (D) 兩者同時呼叫
5. **Q5**: 鴨子型別 (Duck Typing) 的核心思想是？
   - (A) 強型別檢查 (B) 關注物件的行為而非其類別 (C) 必須繼承自特定基類 (D) 鴨子物件優先權最高
6. **Q6**: 類別方法 (`@classmethod`) 的第一個參數約定俗稱為？
   - (A) self (B) this (C) cls (D) base
7. **Q7**: 如果子類別想要完全取代父類別的方法，應該怎麼做？
   - (A) 使用不同的方法名 (B) 在子類別定義同名方法且不呼叫 `super()` (C) 刪除父類別的方法 (D) 使用 `global` 關鍵字
8. **Q8**: 哪一個內建函式可以檢查一個類別是否為另一個類別的子類別？
   - (A) `isinstance()` (B) `issubclass()` (C) `hasattr()` (D) `type()`
9. **Q9**: 在多重繼承中，若出現菱形繼承 (Diamond Inheritance)，Python 如何避免父類別被重複初始化？
   - (A) 使用 `super()` (B) 禁制多重繼承 (C) 自動忽略重複項 (D) 手動刪除冗餘路徑
10. **Q10**: 靜態方法的主要用途是？
    - (A) 修改類別狀態 (B) 實現工廠模式 (C) 將邏輯上屬於該類別但不需要存取狀態的函式進行分組 (D) 提高程式執行速度

### 2. 多選題 (Multiple Choice) - 10 題

11. **Q11**: 繼承的主要目的是？
    - (A) 程式碼重用 (B) 建立層級關係 (C) 增加程式碼複雜度 (D) 減少重複定義
12. **Q12**: 關於 `classmethod` 與 `staticmethod` 的差異，哪些正確？
    - (A) `classmethod` 可以存取類別變數 (B) `staticmethod` 不能存取實體變數 (C) `classmethod` 常被用來實作 Factory Methods (D) 兩者都可以透過實例或類別名呼叫
13. **Q13**: 關於 MRO，哪些描述是正確的？
    - (A) 它是 Method Resolution Order 的縮寫 (B) 它保證了搜尋的單調性 (C) Python 2 與 Python 3 的 MRO 演算法完全相同 (D) 它是線性化的搜尋列表
14. **Q14**: 下列哪些是「多型」的例子？
    - (A) 不同物件都有 `draw()` 方法，呼叫時呈現不同圖形 (B) `+` 號可以用於數字加法與字串拼接 (C) 同一個函式可以接收不同類型的物件並執行其方法 (D) 一個類別繼承多個父類別
15. **Q15**: 使用 `super()` 的優點包括？
    - (A) 避免硬編碼父類別名稱 (B) 支援協同多重繼承 (C) 執行效率比直接呼叫類別名快 (D) 簡化程式碼維護
16. **Q16**: 哪些情況下應該避免使用多重繼承？
    - (A) 類別關係過於複雜 (B) 會產生意義不明的命名衝突 (C) 僅為了獲取一個小功能 (D) 所有 OOP 專案都應該全面禁止多重繼承
17. **Q17**: 關於 Duck Typing，哪些敘述正確？
    - (A) 它增加了程式碼的靈活性 (B) 它可能會導致執行時期錯誤（如果方法不存在） (C) 它與 Java 的介面 (Interface) 機制完全相同 (D) 它是動態語言的特徵
18. **Q18**: 若類別 B 繼承自 A，則？
    - (A) B 擁有 A 的所有公有方法 (B) `issubclass(B, A)` 為 True (C) `isinstance(B(), A)` 為 True (D) B 必須實作 A 的所有方法
19. **Q19**: 在 Python 中，哪些可以視為「可呼叫物件」(Callable)？
    - (A) 函式 (B) 定義了 `__call__` 的類別實例 (C) 類別本身 (D) 字串
20. **Q20**: 關於抽象基類 (ABC)，哪些是正確的？（預習題）
    - (A) 它們不能被實例化 (B) 它們用於定義介面規範 (C) 使用 `abc` 模組實作 (D) 旨在強制子類別實作特定方法

### 3. 填充題 (Fill-in-the-blank) - 10 題

21. Python 3 採用的 MRO 演算法名稱是 __________。
22. 想要在子類別中呼叫父類別的 `__init__`，標準語法是 `super().__________(...)`。
23. 使用裝飾器 __________ 建立的方法，其第一個參數通常命名為 `cls`。
24. 在多型中，不同物件對同一訊息做出不同響應的能力稱為 __________。
25. __________ 繼承是指一個子類別同時擁有多個父類別。
26. 如果想禁止某個方法在子類別被覆蓋，Python 原生 __________ (支援/不支援) 此關鍵字（如 Java 的 final）。
27. 檢查 `a` 是否為 `list` 型別的推薦方法是 `isinstance(a, __________)`。
28. 混入類別 (Mixin) 通常旨在提供 __________ (單一/全部) 功能，且不建議單獨實例化。
29. 在 MRO 列表中，最末端的類別永遠是 __________。
30. 如果子類別定義了與父類別同名的方法，這稱為方法 __________ (Overriding)。

---

## 五、 Q 矩陣 (Q-matrix)

| 題號 | KP 11.1 (繼承) | KP 11.2 (MRO) | KP 11.3 (super) | KP 11.4 (多型) | KP 11.5 (方法型態) |
|---|:---:|:---:|:---:|:---:|:---:|
| Q1 | | 1 | | | |
| Q2 | | | 1 | | |
| Q3 | | | | | 1 |
| Q4 | | 1 | | | |
| Q5 | | | | 1 | |
| Q6 | | | | | 1 |
| Q7 | 1 | | | | |
| Q8 | 1 | | | | |
| Q9 | | | 1 | | |
| Q10 | | | | | 1 |
| Q11 | 1 | | | | |
| Q12 | | | | | 1 |
| Q13 | | 1 | | | |
| Q14 | | | | 1 | |
| Q15 | | | 1 | | |
| Q16 | 1 | | | | |
| Q17 | | | | 1 | |
| Q18 | 1 | | | | |
| Q19 | | | | 1 | |
| Q20 | 1 | | | | |
| Q21 | | 1 | | | |
| Q22 | | | 1 | | |
| Q23 | | | | | 1 |
| Q24 | | | | 1 | |
| Q25 | 1 | | | | |
| Q26 | 1 | | | | |
| Q27 | 1 | | | | |
| Q28 | 1 | | | | |
| Q29 | | 1 | | | |
| Q30 | 1 | | | | |
