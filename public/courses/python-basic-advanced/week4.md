# 課程一：第 4 週 - 迴圈控制與自動化任務

本文件包含了第 4 週完整的教學大綱、實作指南以及練習題庫。

---

## 一、 單元講解 (Lecture) - 總計 100 分鐘

### 1. `for` 迴圈與 `range` 函數 (20 min)
*   **迭代器概念 (KP4.1)**：`for` 迴圈用於遍歷序列（字串、列表、range）。
*   **`range()` 詳解**：
    *   `range(stop)`：從 0 到 stop-1。
    *   `range(start, stop)`：從 start 到 stop-1。
    *   `range(start, stop, step)`：包含步長，支援遞減（如 `range(10, 0, -1)`）。
*   **遍歷字串**：逐字元處理。

### 2. `while` 迴圈與條件控制 (20 min)
*   **運作原理 (KP4.2)**：只要條件為 `True` 就持續執行。
*   **無限迴圈 (Infinite Loop)**：
    *   成因：忘記更新循環變數。
    *   應對：使用 `Ctrl+C` (本地) 或停止按鈕 (Colab) 強制結束。
*   **適用場景**：未知循環次數的情況（如：等待用戶正確輸入）。

### 3. 迴圈中斷與跳過 (break/continue) (20 min)
*   **`break` (KP4.3)**：立即終止整個迴圈，跳出循環體。
*   **`continue` (KP4.3)**：跳過本次剩餘代碼，直接進入下一次迭代。
*   **對比**：`break` 是「結束」，`continue` 是「換下一個」。

### 4. 迴圈與 `else` 區塊 (20 min)
*   **獨特語法 (KP4.4)**：
    *   `for...else` / `while...else`。
    *   **觸發條件**：迴圈「正常完成」（沒有被 `break` 中斷）時執行。
*   **應用場景**：搜尋任務。若找到目標並 `break`，則不執行 `else`；若找遍了都沒找到，則執行 `else`。

### 5. 巢狀迴圈與自動化邏輯 (20 min)
*   **巢狀迴圈 (KP4.5)**：迴圈裡面還有迴圈（如：處理二維矩陣、九九乘法表）。
*   **基礎自動化 (KP4.6)**：
    *   批量重命名概念。
    *   數據過濾與累加。
*   **效能注意**：過深的嵌套會導致執行時間呈幾何級數增長。

---

## 二、 動手實作 (Lab) - 總計 50 分鐘

### 實作一：九九乘法表產生器 (15 min)
**任務目標**：練習巢狀迴圈與 `print` 格式控制。
1.  使用兩層 `for` 迴圈。
2.  輸出 1x1 到 9x9 的結果。
3.  格式要求：使用 `end="\t"` 讓結果整齊排列。
    ```python
    for i in range(1, 10):
        for j in range(1, 10):
            print(f"{i}x{j}={i*j}", end="\t")
        print() # 換行
    ```

### 實作二：猜數字遊戲 (20 min)
**任務目標**：綜合運用 `while`、`if` 與 `break`。
1.  電腦隨機產生一個 1-100 的數字。
2.  用戶不斷輸入猜測，直到猜中為止。
3.  提示用戶「太大了」或「太小了」。
4.  猜中後輸出總共猜了幾次。
    ```python
    import random
    target = random.randint(1, 100)
    count = 0
    while True:
        guess = int(input("猜一個數字: "))
        count += 1
        if guess == target:
            print(f"賓果！共猜了 {count} 次")
            break
        elif guess < target:
            print("再大一點")
        else:
            print("再小一點")
    ```

### 實作三：質數判斷器 (15 min)
**任務目標**：練習 `for...else` 語法。
1.  用戶輸入一個大於 1 的整數。
2.  判斷是否為質數 (Prime Number)。
3.  利用 `else` 區塊簡化邏輯。
    ```python
    num = int(input("輸入數字: "))
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            print(f"{num} 不是質數")
            break
    else:
        print(f"{num} 是質數")
    ```

---

## 三、 本週知識點回顧 (KP)
- **KP4.1**: `for` 迴圈與 `range(start, stop, step)` 的各種用法。
- **KP4.2**: `while` 迴圈的布林控制與無限迴圈預防。
- **KP4.3**: `break` (終止) 與 `continue` (跳過) 的行為差異。
- **KP4.4**: 迴圈後接 `else` 區塊的觸發機制。
- **KP4.5**: 巢狀迴圈 (Nested Loops) 的邏輯與執行順序。
- **KP4.6**: 使用迴圈處理簡單的自動化與數據過濾任務。

---

## 四、 課後測驗題庫 (Quiz) - 30 分鐘

### 1. 單選題 (Single Choice) - 共 10 題

1. **Q1**: `range(1, 10, 2)` 產生的序列是？
   - (A) 1, 3, 5, 7, 9 (B) 1, 2, 3...9 (C) 1, 3, 5, 7 (D) 2, 4, 6, 8
   - *答案*: A (KP4.1)

2. **Q2**: 下列哪種情況會導致 `while` 迴圈變成無限迴圈？
   - (A) 條件永遠為 True (B) 使用了 break (C) 使用了 continue (D) 迴圈內有 print
   - *答案*: A (KP4.2)

3. **Q3**: 想要立即跳出目前的迴圈，應使用哪個關鍵字？
   - (A) stop (B) continue (C) break (D) exit
   - *答案*: C (KP4.3)

4. **Q4**: `for i in range(5):` 迴圈會執行幾次？
   - (A) 4 (B) 5 (C) 6 (D) 無限次
   - *答案*: B (KP4.1)

5. **Q5**: 關於迴圈中的 `else` 區塊，何時會被執行？
   - (A) 迴圈被 break 時 (B) 迴圈正常結束時 (C) 迴圈發生錯誤時 (D) 永遠不會執行
   - *答案*: B (KP4.4)

6. **Q6**: 執行 `for i in range(3): continue; print(i)`，會輸出什麼？
   - (A) 0, 1, 2 (B) 2 (C) 什麼都不輸出 (D) 報錯
   - *答案*: C (KP4.3)

7. **Q7**: `range(5, 0, -1)` 的最後一個數字是？
   - (A) 0 (B) 1 (C) -1 (D) 5
   - *答案*: B (KP4.1)

8. **Q8**: 在巢狀迴圈中，`break` 會跳出哪一層？
   - (A) 所有的迴圈 (B) 最外層迴圈 (C) 直接包含它的那一層迴圈 (D) 不會跳出
   - *答案*: C (KP4.3, KP4.5)

9. **Q9**: 下列哪個序列可以用於 `for` 迴圈迭代？
   - (A) 字串 (B) range 物件 (C) 列表 (D) 以上皆可
   - *答案*: D (KP4.1)

10. **Q10**: `while` 迴圈後的條件判斷式，其結果必須是？
    - (A) 整數 (B) 布林值 (或可轉換為布林值) (C) 字串 (D) None
    - *答案*: B (KP4.2)

### 2. 多選題 (Multiple Choice) - 共 10 題

1. **Q11**: 下列哪些是 `range()` 函數的合法調用方式？
   - (A) range(10) (B) range(1, 10) (C) range(1, 10, 0) (D) range(10, 1, -2)
   - *答案*: A, B, D (KP4.1)

2. **Q12**: 關於 `break` 和 `continue` 的描述，正確的有？
   - (A) `break` 用於結束迴圈 (B) `continue` 用於跳過本次迭代 (C) 兩者都只能在迴圈內使用 (D) 兩者功能完全一樣
   - *答案*: A, B, C (KP4.3)

3. **Q13**: 哪些方法可以防止 `while` 迴圈變成死循環？
   - (A) 在內部修改條件變數 (B) 使用 `break` 設定退出點 (C) 確保初始條件最終會變為 False (D) 刪除迴圈
   - *答案*: A, B, C (KP4.2)

4. **Q14**: 巢狀迴圈常見的應用有哪些？
   - (A) 矩陣運算 (B) 遍歷二維列表 (C) 產生排列組合 (D) 簡單的單行輸出
   - *答案*: A, B, C (KP4.5)

5. **Q15**: 下列哪些是有效的 Python 迴圈結構？
   - (A) for...in (B) while (C) do...while (D) repeat...until
   - *答案*: A, B (KP4.1, KP4.2)

6. **Q16**: 在 `for i in range(10):` 中，`i` 的值可能是？
   - (A) 0 (B) 5 (C) 9 (D) 10
   - *答案*: A, B, C (KP4.1)

7. **Q17**: 關於 `for...else` 語法，哪些說法是正確的？
   - (A) 常用於搜尋任務 (B) 如果迴圈被 break，else 不會執行 (C) 如果迴圈沒進入 (如 range(0))，else 會執行 (D) 它取代了 if 語句
   - *答案*: A, B, C (KP4.4)

8. **Q18**: 自動化任務中，迴圈可以用來做什麼？
   - (A) 批次讀取檔案 (B) 定時檢查網站狀態 (C) 自動發送多封郵件 (D) 增加電腦硬體速度
   - *答案*: A, B, C (KP4.6)

9. **Q19**: 下列代碼哪些會進入死循環？
   - (A) `while True: pass` (B) `while 1: pass` (C) `while "False": pass` (D) `while []: pass`
   - *答案*: A, B, C (KP4.2)

10. **Q20**: 在字串 `s = "Python"` 上執行 `for char in s:`，`char` 的值會依次是？
    - (A) P (B) y (C) t (D) n
    - *答案*: A, B, C, D (KP4.1)

### 3. 填充題 (Fill-in-the-blank) - 共 10 題

1. **Q21**: `range(0, 5)` 會產生 __________ 個整數。
   - *答案*: 5 (KP4.1)

2. **Q22**: 在迴圈中，跳過當前這一次迭代並直接開始下一次的關鍵字是 __________。
   - *答案*: continue (KP4.3)

3. **Q23**: 想要在迴圈「正常結束」後執行一段代碼，可以使用 __________ 關鍵字與迴圈對齊。
   - *答案*: else (KP4.4)

4. **Q24**: `for i in range(10, 0, -2)`，第一次迭代的 `i` 值是 __________。
   - *答案*: 10 (KP4.1)

5. **Q25**: 兩層迴圈嵌套在一起，若內層執行 3 次，外層執行 4 次，則內層代碼共執行 __________ 次。
   - *答案*: 12 (KP4.5)

6. **Q26**: `while` 迴圈是基於 __________ 判斷來決定是否繼續執行的。
   - *答案*: 條件 (或 布林) (KP4.2)

7. **Q27**: 使用關鍵字 __________ 可以立即強制終止迴圈。
   - *答案*: break (KP4.3)

8. **Q28**: `range(5)` 等同於 `range(0, 5, __________ )`。
   - *答案*: 1 (KP4.1)

9. **Q29**: 在自動化處理中，迴圈遍歷列表並將符合條件的數據挑出來，這稱為數據 __________。
   - *答案*: 過濾 (Filtering) (KP4.6)

10. **Q30**: 若要產生 1 到 100 (包含 100) 的序列，應寫為 `range(1, __________ )`。
    - *答案*: 101 (KP4.1)

---

## 五、 Q 矩陣 (Q-matrix)

| 題號 | KP4.1 | KP4.2 | KP4.3 | KP4.4 | KP4.5 | KP4.6 |
|---|---|---|---|---|---|---|
| Q1 | 1 | 0 | 0 | 0 | 0 | 0 |
| Q2 | 0 | 1 | 0 | 0 | 0 | 0 |
| Q3 | 0 | 0 | 1 | 0 | 0 | 0 |
| Q4 | 1 | 0 | 0 | 0 | 0 | 0 |
| Q5 | 0 | 0 | 0 | 1 | 0 | 0 |
| Q6 | 0 | 0 | 1 | 0 | 0 | 0 |
| Q7 | 1 | 0 | 0 | 0 | 0 | 0 |
| Q8 | 0 | 0 | 1 | 0 | 1 | 0 |
| Q9 | 1 | 0 | 0 | 0 | 0 | 0 |
| Q10| 0 | 1 | 0 | 0 | 0 | 0 |
| Q11| 1 | 0 | 0 | 0 | 0 | 0 |
| Q12| 0 | 0 | 1 | 0 | 0 | 0 |
| Q13| 0 | 1 | 1 | 0 | 0 | 0 |
| Q14| 0 | 0 | 0 | 0 | 1 | 0 |
| Q15| 1 | 1 | 0 | 0 | 0 | 0 |
| Q16| 1 | 0 | 0 | 0 | 0 | 0 |
| Q17| 0 | 0 | 0 | 1 | 0 | 0 |
| Q18| 0 | 0 | 0 | 0 | 0 | 1 |
| Q19| 0 | 1 | 0 | 0 | 0 | 0 |
| Q20| 1 | 0 | 0 | 0 | 0 | 0 |
| Q21| 1 | 0 | 0 | 0 | 0 | 0 |
| Q22| 0 | 0 | 1 | 0 | 0 | 0 |
| Q23| 0 | 0 | 0 | 1 | 0 | 0 |
| Q24| 1 | 0 | 0 | 0 | 0 | 0 |
| Q25| 0 | 0 | 0 | 0 | 1 | 0 |
| Q26| 0 | 1 | 0 | 0 | 0 | 0 |
| Q27| 0 | 0 | 1 | 0 | 0 | 0 |
| Q28| 1 | 0 | 0 | 0 | 0 | 0 |
| Q29| 0 | 0 | 0 | 0 | 0 | 1 |
| Q30| 1 | 0 | 0 | 0 | 0 | 0 |
