# 課程一：第 12 週 - 進階函式與裝飾器 (Decorators)

本週我們將深入探討 Python 中「函式是一等公民」的極致應用。從閉包的記憶機制到裝飾器的切面編程 (AOP)，學習如何優雅地擴充現有程式碼的功能而不修改其核心邏輯。

---

## 一、 單元講解 (Lecture) - 100 分鐘

### 1. 閉包 (Closures) 與 `nonlocal` 關鍵字
- **閉包的定義**：一個能記住其定義時環境（作用域）的函式。
- **自由變數 (Free Variables)**：存在於外部函式但被內部函式引用的變數。
- **`nonlocal`**：當內部函式需要修改外部（非全域）變數時的聲明。

### 2. 裝飾器 (Decorators) 的基本原理
- **語法糖 `@`**：裝飾器本質上是一個接收函式並回傳新函式的高階函式。
- **基本結構**：`outer` -> `inner(*args, **kwargs)` -> `return inner`。
- **執行時機**：裝飾器是在函式定義時執行的，而非呼叫時。

### 3. 帶參數的裝飾器 (Nested Decorators)
- **需求**：如果我們希望裝飾器本身能接收參數（例如：`@retry(times=3)`）。
- **三層結構**：最外層接收參數，中間層接收函式，最內層處理執行邏輯。

### 4. `functools.wraps` 的重要性
- **副作用**：被裝飾後的函式，其 `__name__` 與 `__doc__` 會變成裝飾器的內部函式名。
- **解決方案**：使用 `@functools.wraps(func)` 裝飾內部函式，以保留原始函式的元資料 (Metadata)。

### 5. 裝飾類別中的方法
- **實體方法裝飾**：注意 `self` 參數的傳遞。
- **多重裝飾器**：疊加順序（由內而外裝飾，由上而下執行）。

---

## 二、 動手實作 (Lab) - 50 分鐘

### 任務：開發「系統監控與權限裝飾器組」
1.  **實作 `@timer` 裝飾器**：
    - 紀錄函式的開始與結束時間，印出執行耗時。
    - 使用 `functools.wraps`。
2.  **實作 `@debug` 裝飾器**：
    - 印出函式的名稱及其收到的所有參數值。
3.  **實作 `@rate_limit(seconds)` 裝飾器**：
    - 限制函式在指定秒數內只能執行一次，否則印出 "Too fast!"。
    - 提示：需結合「閉包」來紀錄上次執行的時間戳。
4.  **綜合應用**：
    - 同時在一個計算密集型函式上套用多個裝飾器，觀察輸出順序。

---

## 三、 本週知識點回顧 (KP)

- **[KP 12.1] 閉包機制**：函式與環境的綁定，使得變數在外部函式結束後仍能存活。
- **[KP 12.2] 裝飾器本質**：`func = decorator(func)` 的語法簡寫，實現非侵入式功能擴充。
- **[KP 12.3] 參數解包應用**：利用 `*args` 與 `**kwargs` 確保裝飾器能相容任何參數配置。
- **[KP 12.4] 元資料保護**：習慣性使用 `wraps` 以維持偵錯時的可讀性。
- **[KP 12.5] 帶參裝飾器邏輯**：多一層閉包封裝，用於動態調整裝飾器行為。

---

## 四、 課後測驗題庫 (Quiz) - 30 分鐘

### 1. 單選題 (Single Choice) - 10 題

1. **Q1**: 關於閉包 (Closure)，下列哪項敘述是錯誤的？
   - (A) 必須有巢狀函式 (B) 內部函式必須引用外部函式的變數 (C) 外部函式必須回傳內部函式 (D) 外部函式的變數必須是全域變數
2. **Q2**: 在裝飾器中，為了讓它能裝飾任何參數數量的函式，通常會使用？
   - (A) `(args, kwargs)` (B) `(*args, **kwargs)` (C) `(self, *args)` (D) `(func)`
3. **Q3**: 執行 `@dec\ndef func(): pass` 相當於執行？
   - (A) `func = dec(func)` (B) `dec = func(dec)` (C) `dec(func())` (D) `func = dec()`
4. **Q4**: `nonlocal` 關鍵字的作用是？
   - (A) 聲明一個全域變數 (B) 修改上一層非全域作用域中的變數 (C) 在類別中定義私有屬性 (D) 讓變數在模組間共享
5. **Q5**: 為什麼推薦在裝飾器內使用 `functools.wraps`？
   - (A) 提升執行速度 (B) 防止記憶體洩漏 (C) 保留原始函式的名稱與文件字串 (D) 讓裝飾器支援異步呼叫
6. **Q6**: 若一個函式被兩個裝飾器 `@a` 與 `@b` 修飾（`@a` 在上），其執行包裝順序為？
   - (A) `a(b(func))` (B) `b(a(func))` (C) 同時執行 (D) 隨機執行
7. **Q7**: 帶參數的裝飾器（如 `@repeat(3)`）通常需要幾層巢狀函式？
   - (A) 1 層 (B) 2 層 (C) 3 層 (D) 4 層
8. **Q8**: 下列何者不是裝飾器的典型應用場景？
   - (A) 紀錄日誌 (B) 權限驗證 (C) 效能測試 (D) 數值計算核心邏輯
9. **Q9**: 在閉包中，被引用的外部變數存放在函式物件的哪一個屬性裡？
   - (A) `__globals__` (B) `__closure__` (C) `__dict__` (D) `__code__`
10. **Q10**: 裝飾器是在什麼時候被執行的？
    - (A) 模組匯入時 (B) 函式定義時 (C) 第一次呼叫函式時 (D) 每次呼叫函式時

### 2. 多選題 (Multiple Choice) - 10 題

11. **Q11**: 閉包形成的必要條件包括？
    - (A) 存在於巢狀函式結構中 (B) 內部函式引用了外部函式的局部變數 (C) 外部函式回傳了內部函式 (D) 外部函式必須使用 `lambda`
12. **Q12**: 關於裝飾器的描述，哪些正確？
    - (A) 它是一種設計模式 (B) 它可以修改函式的回傳值 (C) 它可以完全取代原始函式 (D) 它只能用於函式，不能用於類別
13. **Q13**: 使用 `@functools.wraps` 可以保留原始函式的哪些資訊？
    - (A) `__name__` (B) `__doc__` (C) `__annotations__` (D) 區域變數
14. **Q14**: 下列哪些是高階函式的特徵？
    - (A) 接收函式作為參數 (B) 回傳一個函式 (C) 函式內部定義另一個函式 (D) 函式名稱是大寫開頭
15. **Q15**: 關於 `nonlocal` 與 `global`，哪些正確？
    - (A) `global` 作用於模組層級 (B) `nonlocal` 至少需要兩層巢狀函式 (C) 它們都可以解決作用域遮蔽 (Shadowing) 問題 (D) 兩者可以同時作用於同一個變數
16. **Q16**: 裝飾器可以用於哪些物件？
    - (A) 普通函式 (B) 類別方法 (C) 類別本身 (D) 內建關鍵字
17. **Q17**: 帶參數的裝飾器結構中，各層職責描述正確的是？
    - (A) 最外層：接收裝飾器參數 (B) 中間層：接收被裝飾的函式 (C) 最內層：實作包裝邏輯 (D) 每一層都必須使用 `*args`
18. **Q18**: 關於多重裝飾器的執行順序，哪些敘述正確？
    - (A) 包裝順序是由下而上 (B) 包裝順序是由上而下 (C) 呼叫時的進入順序是由上而下 (D) 呼叫時的離開順序是由下而上
19. **Q19**: 閉包的主要用途包括？
    - (A) 資料隱藏（類似私有變數） (B) 減少全域變數的使用 (C) 實作裝飾器 (D) 提高迴圈執行效率
20. **Q20**: 下列哪些裝飾器是 Python 內建的？
    - (A) `@property` (B) `@classmethod` (C) `@wraps` (D) `@timer`

### 3. 填充題 (Fill-in-the-blank) - 10 題

21. 當內部函式想要修改外部函式的變數時，必須使用 __________ 關鍵字。
22. 裝飾器的本質是一個 __________ (High-order) 函式。
23. 為了獲取原始函式 `f` 的名稱，可以使用 `f.__________` 屬性。
24. 在帶參數的裝飾器中，最外層函式回傳的是 __________ 函式。
25. 若裝飾器不回傳任何物件，則被裝飾的函式名將變為 __________。
26. 閉包中的「自由變數」是指那些被引用但未在 __________ 函式中定義的變數。
27. `@` 符號在裝飾器語境下被稱為 __________ (Syntactic Sugar)。
28. 匯入 `wraps` 裝飾器需要從 __________ 模組中匯入。
29. 如果裝飾器 `dec` 裡面定義了 `wrapper` 函式，通常最後要執行 `return __________`。
30. 類別方法 `@classmethod` 本質上也是一種 __________。

---

## 五、 Q 矩陣 (Q-matrix)

| 題號 | KP 12.1 (閉包) | KP 12.2 (裝飾器本質) | KP 12.3 (解包參數) | KP 12.4 (元資料) | KP 12.5 (帶參裝飾器) |
|---|:---:|:---:|:---:|:---:|:---:|
| Q1 | 1 | | | | |
| Q2 | | | 1 | | |
| Q3 | | 1 | | | |
| Q4 | 1 | | | | |
| Q5 | | | | 1 | |
| Q6 | | 1 | | | |
| Q7 | | | | | 1 |
| Q8 | | 1 | | | |
| Q9 | 1 | | | | |
| Q10 | | 1 | | | |
| Q11 | 1 | | | | |
| Q12 | | 1 | | | |
| Q13 | | | | 1 | |
| Q14 | 1 | 1 | | | |
| Q15 | 1 | | | | |
| Q16 | | 1 | | | |
| Q17 | | | | | 1 |
| Q18 | | 1 | | | |
| Q19 | 1 | | | | |
| Q20 | | 1 | | | |
| Q21 | 1 | | | | |
| Q22 | | 1 | | | |
| Q23 | | | | 1 | |
| Q24 | | | | | 1 |
| Q25 | | 1 | | | |
| Q26 | 1 | | | | |
| Q27 | | 1 | | | |
| Q28 | | | | 1 | |
| Q29 | | 1 | | | |
| Q30 | | 1 | | | |
