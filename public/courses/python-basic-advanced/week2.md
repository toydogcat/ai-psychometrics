# 課程一：第 2 週 - 變數、資料型態與基礎運算

本文件包含了第 2 週完整的教學大綱、實作指南以及練習題庫。

---

## 一、 單元講解 (Lecture) - 總計 100 分鐘

### 1. 變數賦值與記憶體模型 (20 min)
*   **賦值的本質 (KP2.1)**：在 Python 中，變數更像是「標籤」而非「容器」。賦值是將標籤貼在記憶體中的物件上。
*   **關鍵函數**：
    *   `id()`：獲取物件在記憶體中的唯一標識（地址）。
    *   `type()`：查看變數目前指向的物件型態。
*   **動態型別**：同一個變數名可以先指向整數，再指向字串。

### 2. 數值型態與算術運算 (20 min)
*   **常用數值 (KP2.2)**：
    *   `int` (整數)：無長度限制。
    *   `float` (浮點數)：科學計數法與精度問題。
*   **運算符號**：
    *   基礎：`+`, `-`, `*`, `/` (除法永遠返回浮點數)。
    *   進階：`//` (整除), `%` (取餘數), `**` (次方)。
*   **複合賦值**：`x += 1`, `y *= 2`。

### 3. 字串操作與切片 (25 min)
*   **索引與切片 (KP2.3)**：
    *   正向索引從 0 開始，反向索引從 -1 開始。
    *   語法：`s[start:stop:step]` (包含 start，不包含 stop)。
*   **常用方法**：
    *   `.strip()`：去除兩端空格。
    *   `.lower()`, `.upper()`：大小寫轉換。
    *   `.split()`, `.join()`：字串切割與連結。
    *   `.replace()`：替換內容。

### 4. 布林邏輯與比較運算 (20 min)
*   **布林值 (KP2.4)**：`True` 與 `False` (首字母必須大寫)。
*   **比較運算**：`==`, `!=`, `<`, `>`, `<=`, `>=`。
*   **邏輯運算**：`and` (且), `or` (或), `not` (非)。
*   **Truthy & Falsy**：在 Python 中，`0`, `""`, `[]`, `None` 在布林環境下都被視為 `False`。

### 5. 型別轉換與內建函數 (15 min)
*   **顯式轉換 (KP2.5)**：`int()`, `float()`, `str()`, `bool()`。
*   **常用函數 (KP2.6)**：
    *   `len()`：計算長度（字串、容器）。
    *   `round()`：四捨五入。
    *   `abs()`：絕對值。

---

## 二、 動手實作 (Lab) - 總計 50 分鐘

### 實作一：變數追蹤與記憶體觀察 (15 min)
**任務目標**：理解變數指向物件的概念。
1.  在 Colab 中輸入以下代碼並觀察輸出：
    ```python
    a = [1, 2, 3]
    b = a
    print(f"a id: {id(a)}, b id: {id(b)}")
    print(f"a is b: {a is b}")
    
    a.append(4)
    print(f"b contents: {b}") # 觀察 b 是否跟著改變
    ```
2.  思考：為什麼修改 `a` 會影響到 `b`？

### 實作二：字串魔術師 (20 min)
**任務目標**：練習字串切片與方法。
1.  給定一個髒字串：`raw_data = "  Python_Programming_Is_Fun  "`。
2.  任務：
    - 去除首尾空格。
    - 將所有字母轉為小寫。
    - 將底線 `_` 替換為空格。
    - 取得「programming」這個單詞。
    ```python
    # 參考解答
    cleaned = raw_data.strip().lower().replace("_", " ")
    words = cleaned.split()
    print(words[1])
    ```

### 實作三：單位轉換器 (15 min)
**任務目標**：綜合運用數值運算與型別轉換。
1.  編寫程式，讓用戶輸入攝氏溫度 (Celsius)。
2.  計算並輸出華氏溫度 (Fahrenheit)，公式為：`F = C * 9/5 + 32`。
3.  輸出結果需四捨五入到小數點後一位。

---

## 三、 本週知識點回顧 (KP)
- **KP2.1**: 變數賦值、`id()` 與記憶體模型。
- **KP2.2**: 數值型態 (int, float) 與算術運算 (含 //, %, **)。
- **KP2.3**: 字串切片 `[start:stop:step]` 與常用字串方法。
- **KP2.4**: 布林邏輯與 Truthy/Falsy 概念。
- **KP2.5**: 顯式型別轉換 (Type Casting)。
- **KP2.6**: 基礎內建函數 (`len`, `round`, `abs`)。

---

## 四、 課後測驗題庫 (Quiz) - 30 分鐘

### 1. 單選題 (Single Choice) - 共 10 題

1. **Q1**: 執行 `x = 5; y = x; x = 10` 後，`y` 的值是多少？
   - (A) 5 (B) 10 (C) None (D) 報錯
   - *答案*: A (KP2.1)

2. **Q2**: `type(3.14)` 的返回值是？
   - (A) <class 'int'> (B) <class 'str'> (C) <class 'float'> (D) <class 'bool'>
   - *答案*: C (KP2.2)

3. **Q3**: 表達式 `10 // 3` 的結果是？
   - (A) 3.333 (B) 3 (C) 1 (D) 0
   - *答案*: B (KP2.2)

4. **Q4**: 若 `s = "Python"`, 則 `s[1:4]` 的結果是？
   - (A) Pyt (B) yth (C) ytho (D) ythn
   - *答案*: B (KP2.3)

5. **Q5**: 下列哪個值在 `bool()` 轉換後為 `True`？
   - (A) 0 (B) "" (C) "False" (D) None
   - *答案*: C (KP2.4, KP2.5)

6. **Q6**: 獲取物件記憶體地址的函數是？
   - (A) address() (B) loc() (C) id() (D) mem()
   - *答案*: C (KP2.1)

7. **Q7**: `round(2.675, 2)` 在 Python 中的行為可能與預期不同，主要是因為？
   - (A) 四捨六入五成雙 (B) 浮點數精度問題 (C) round 函數壞了 (D) 它會無條件進位
   - *答案*: B (KP2.2, KP2.6)

8. **Q8**: 如何檢查字串 `s` 是否全為大寫？
   - (A) s.isUpper() (B) s.isupper() (C) s.UPPER() (D) s.upper()
   - *答案*: B (KP2.3)

9. **Q9**: `2 ** 3` 的結果是？
   - (A) 6 (B) 8 (C) 9 (D) 5
   - *答案*: B (KP2.2)

10. **Q10**: `len("Hello\n")` 的長度是？
    - (A) 5 (B) 6 (C) 7 (D) 4
    - *答案*: B (KP2.6)

### 2. 多選題 (Multiple Choice) - 共 10 題

1. **Q11**: 關於 Python 變數，哪些描述正確？
   - (A) 變數名稱區分大小寫 (B) 可以使用數字開頭 (C) 變數是物件的引用 (D) 無須宣告型別
   - *答案*: A, C, D (KP2.1)

2. **Q12**: 下列哪些是字串切片的正確語法？
   - (A) s[0] (B) s[::-1] (C) s[1:5:2] (D) s(0:5)
   - *答案*: A, B, C (KP2.3)

3. **Q13**: 下列哪些值被視為 Falsy (布林值為 False)？
   - (A) 0 (B) 0.0 (C) " " (D) []
   - *答案*: A, B, D (KP2.4)

4. **Q14**: 字串方法中，哪些會回傳一個**新**字串 (而非修改原字串)？
   - (A) .replace() (B) .strip() (C) .lower() (D) 所有的字串方法 (因為字串不可變)
   - *答案*: A, B, C, D (KP2.3)

5. **Q15**: 下列運算結果為 `True` 的有哪些？
   - (A) 3 > 2 and 5 < 10 (B) not (5 == 5) (C) 10 != 10 or 1 > 0 (D) True and False
   - *答案*: A, C (KP2.4)

6. **Q16**: 下列哪些可以用於將字串 "123" 轉換為數字？
   - (A) int("123") (B) float("123") (C) str("123") (D) eval("123")
   - *答案*: A, B, D (KP2.5)

7. **Q17**: 關於浮點數 `0.1 + 0.2 == 0.3`，哪些描述正確？
   - (A) 結果為 True (B) 結果為 False (C) 這是因為二進位表示法的精度限制 (D) 應使用 math.isclose() 來比較
   - *答案*: B, C, D (KP2.2)

8. **Q18**: 下列哪些是有效的 Python 賦值語句？
   - (A) x, y = 1, 2 (B) x = y = z = 0 (C) x += 5 (D) 5 = x
   - *答案*: A, B, C (KP2.1, KP2.2)

9. **Q19**: 下列哪些函數屬於 Python 內建函數？
   - (A) print() (B) len() (C) type() (D) id()
   - *答案*: A, B, C, D (KP2.6, KP2.1)

10. **Q20**: 使用 `s.split(",")` 處理字串 `"apple,banana,cherry"`，結果會是？
    - (A) 一個串列 (List) (B) 包含三個元素 (C) 原字串會被改變 (D) 逗號會消失
    - *答案*: A, B, D (KP2.3)

### 3. 填充題 (Fill-in-the-blank) - 共 10 題

1. **Q21**: `10 % 3` 的餘數是 __________。
   - *答案*: 1 (KP2.2)

2. **Q22**: 在 Python 中，字串是不可變的 (Immutable)，這意味著你不能直接修改字串中的某個字元。 (是非題答案請填「是」或「否」) __________。
   - *答案*: 是 (KP2.3)

3. **Q23**: 想要將 `3.9` 轉換為整數 `3`，應使用 __________ 函數。
   - *答案*: int() (KP2.5)

4. **Q24**: 邏輯運算符中，表示「且」的關鍵字是 __________。
   - *答案*: and (KP2.4)

5. **Q25**: 字串方法 __________ 可以用來檢查字串是否以特定的子字串開頭。
   - *答案*: startswith() (KP2.3)

6. **Q26**: 賦值語句 `x = "100"`，此時 `type(x)` 的結果是 __________。
   - *答案*: <class 'str'> (KP2.1, KP2.3)

7. **Q27**: `abs(-10)` 的結果是 __________。
   - *答案*: 10 (KP2.6)

8. **Q28**: 切片語句 `s[::2]` 中的數字 2 代表 __________ (步長)。
   - *答案*: step (或 步長) (KP2.3)

9. **Q29**: 布林值只有兩個，分別是 `True` 和 __________。
   - *答案*: False (KP2.4)

10. **Q30**: `str(100) + "20"` 的結果是字串 __________。
    - *答案*: 10020 (KP2.3, KP2.5)

---

## 五、 Q 矩陣 (Q-matrix)

| 題號 | KP2.1 | KP2.2 | KP2.3 | KP2.4 | KP2.5 | KP2.6 |
|---|---|---|---|---|---|---|
| Q1 | 1 | 0 | 0 | 0 | 0 | 0 |
| Q2 | 0 | 1 | 0 | 0 | 0 | 0 |
| Q3 | 0 | 1 | 0 | 0 | 0 | 0 |
| Q4 | 0 | 0 | 1 | 0 | 0 | 0 |
| Q5 | 0 | 0 | 0 | 1 | 1 | 0 |
| Q6 | 1 | 0 | 0 | 0 | 0 | 0 |
| Q7 | 0 | 1 | 0 | 0 | 0 | 1 |
| Q8 | 0 | 0 | 1 | 0 | 0 | 0 |
| Q9 | 0 | 1 | 0 | 0 | 0 | 0 |
| Q10| 0 | 0 | 0 | 0 | 0 | 1 |
| Q11| 1 | 0 | 0 | 0 | 0 | 0 |
| Q12| 0 | 0 | 1 | 0 | 0 | 0 |
| Q13| 0 | 0 | 0 | 1 | 0 | 0 |
| Q14| 0 | 0 | 1 | 0 | 0 | 0 |
| Q15| 0 | 0 | 0 | 1 | 0 | 0 |
| Q16| 0 | 0 | 0 | 0 | 1 | 0 |
| Q17| 0 | 1 | 0 | 0 | 0 | 0 |
| Q18| 1 | 1 | 0 | 0 | 0 | 0 |
| Q19| 1 | 0 | 0 | 0 | 0 | 1 |
| Q20| 0 | 0 | 1 | 0 | 0 | 0 |
| Q21| 0 | 1 | 0 | 0 | 0 | 0 |
| Q22| 0 | 0 | 1 | 0 | 0 | 0 |
| Q23| 0 | 0 | 0 | 0 | 1 | 0 |
| Q24| 0 | 0 | 0 | 1 | 0 | 0 |
| Q25| 0 | 0 | 1 | 0 | 0 | 0 |
| Q26| 1 | 0 | 1 | 0 | 0 | 0 |
| Q27| 0 | 0 | 0 | 0 | 0 | 1 |
| Q28| 0 | 0 | 1 | 0 | 0 | 0 |
| Q29| 0 | 0 | 0 | 1 | 0 | 0 |
| Q30| 0 | 0 | 1 | 0 | 1 | 0 |
